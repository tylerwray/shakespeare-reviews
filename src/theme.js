import { createMuiTheme } from '@material-ui/core'

const options = {
  palette: {
    primary: { main: '#5D1049', extraLight: '#F8EEF4' },
    secondary: { main: '#FF7569' }
  }
}

export default createMuiTheme(options)
